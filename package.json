{
  "name": "addon-qrcode",
  "version": "1.0.1",
  "description": "新意二维码助手 — 本地二维码生成与解析（Edge/Chrome MV3）",
  "scripts": {
    "build": "esbuild src/popup.js --bundle --outfile=dist/popup.js --format=iife --platform=browser --minify",
    "watch": "esbuild src/popup.js --bundle --outfile=dist/popup.js --format=iife --platform=browser --watch"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "jsqr": "^1.4.0",
    "qrcode": "^1.5.4",
    "utif": "^3.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
